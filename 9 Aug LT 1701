class Solution:
    def averageWaitingTime(self, customers):
        current_time = 0
        total_waiting = 0

        for arrival, time in customers:
            start = max(arrival, current_time)
            finish = start + time

            total_waiting += finish - arrival
            current_time = finish

        return total_waiting / len(customers)
