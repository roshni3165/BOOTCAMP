class Solution(object):
    def productExceptSelf(self, nums):
        n=len(nums)
        ans=[1]*n
        s=1
        for i in range(1,n):+
            ans[i]=nums[i-1]*ans[i-1]
        for j in range(n-2,-1,-1):
            s=s*nums[j+1]
            ans[j]=ans[j]*s
        return ans
        
