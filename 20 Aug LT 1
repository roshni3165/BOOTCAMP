class Solution(object):
    def twoSum(self, nums, target):
        m={}
        for i in range(len(nums)):
            x=target-nums[i]
            if x in m:
                return [m[x],i]
            m[nums[i]]=i
            
            
